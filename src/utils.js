const DATE_FORMAT = 'YYYY-MM-DD';
const isObject = v => v && ['object', 'function'].includes(typeof v);
const isArray = Array.isArray;

module.exports = {
    DATE_FORMAT,
    isObject,
    isArray,
};